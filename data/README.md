# Data Directory

This directory stores transaction datasets for the fraud detection system.

- `raw/`: Immutable raw transaction logs and datasets.
- `processed/`: Cleaned, feature-engineered datasets ready for model training and evaluation.

> Note: Raw transaction files with sensitive financial PII must never be committed to source control.
